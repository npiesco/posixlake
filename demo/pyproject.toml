[project]
name = "posixlake-cli-demo"
version = "0.1.0"
description = "Recorded Windows + WSL posixlake CLI parity demo"
requires-python = ">=3.13"
dependencies = [
  "azure-cognitiveservices-speech>=1.43.0",
]

[tool.uv]
package = false
